{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:industrial-shadow:production-closure-input:v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "status", "approval", "gates", "red_lines", "artifacts", "attestations", "signatories"],
  "properties": {
    "schema_version": {"const": 2},
    "status": {"const": "verified"},
    "approval": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schema_version", "acceptance_run_id", "release_digest", "trust_store_digest", "gate_digests", "attestation_digests", "release_coordinates", "scope", "exclusions", "approval_digest"],
      "properties": {
        "schema_version": {"const": 2},
        "acceptance_run_id": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{7,127}$"},
        "release_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
        "trust_store_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
        "gate_digests": {
          "type": "object", "minProperties": 17, "maxProperties": 17,
          "additionalProperties": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
        },
        "attestation_digests": {
          "type": "object", "minProperties": 4, "maxProperties": 4,
          "additionalProperties": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
        },
        "release_coordinates": {
          "type": "object", "additionalProperties": false,
          "required": ["candidate_image", "build_digest", "simulator_build_digest", "environment_digest", "deployment_plan_digest"],
          "properties": {
            "candidate_image": {"type": "string", "pattern": "^[^@\\s]+@sha256:[a-f0-9]{64}$"},
            "build_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
            "simulator_build_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
            "environment_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
            "deployment_plan_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
          }
        },
        "scope": {"type": "array", "minItems": 3, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
        "exclusions": {"type": "array", "minItems": 3, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
        "approval_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
      }
    },
    "gates": {
      "type": "object",
      "additionalProperties": false,
      "required": ["production_preflight", "supply_chain", "postgresql_migration", "backup_restore", "oidc", "s3", "opcua_interoperability", "network_policy", "container_scan", "security", "resilience", "performance", "privacy", "accessibility", "upgrade_rollback", "benchmark_150"],
      "properties": {
        "production_preflight": {"const": "PASSED"},
        "supply_chain": {"const": "PASSED"},
        "postgresql_migration": {"const": "PASSED"},
        "backup_restore": {"const": "PASSED"},
        "oidc": {"const": "PASSED"},
        "s3": {"const": "PASSED"},
        "opcua_interoperability": {"const": "PASSED"},
        "network_policy": {"const": "PASSED"},
        "container_scan": {"const": "PASSED"},
        "security": {"const": "PASSED"},
        "resilience": {"const": "PASSED"},
        "performance": {"const": "PASSED"},
        "privacy": {"const": "PASSED"},
        "accessibility": {"const": "PASSED"},
        "upgrade_rollback": {"const": "PASSED"},
        "benchmark_150": {"const": "PASSED"}
      }
    },
    "red_lines": {
      "type": "object",
      "additionalProperties": false,
      "required": ["real_write_attempts", "unauthorized_actions", "gold_exposures"],
      "properties": {
        "real_write_attempts": {"const": 0},
        "unauthorized_actions": {"const": 0},
        "gold_exposures": {"const": 0}
      }
    },
    "artifacts": {
      "type": "array",
      "minItems": 17,
      "maxItems": 17,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["gate", "path", "sha256", "evidence_digest"],
        "properties": {
          "gate": {"enum": ["preflight", "supply_chain", "postgresql_migration", "oidc", "backup_restore", "s3", "external_ca", "real_ot", "network_policy", "container_scan", "security", "resilience", "performance", "privacy", "accessibility", "upgrade_rollback", "benchmark_150"]},
          "path": {"type": "string"},
          "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "evidence_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
        }
      }
    },
    "attestations": {
      "type": "array",
      "minItems": 4,
      "maxItems": 4,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["gate", "report_path", "report_sha256", "report_digest", "trust_store_digest", "artifacts"],
        "properties": {
          "gate": {"enum": ["security", "privacy", "accessibility", "benchmark_150"]},
          "report_path": {"type": "string", "minLength": 1},
          "report_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "report_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "trust_store_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "artifacts": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["path", "sha256"],
              "properties": {
                "path": {"type": "string", "minLength": 1},
                "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
              }
            }
          }
        }
      }
    },
    "signatories": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["identity", "role", "approved", "signed_at", "approval_digest", "public_key_b64", "signature_b64"],
        "properties": {
          "identity": {"type": "string", "minLength": 1},
          "role": {"enum": ["release_owner", "security_owner"]},
          "approved": {"const": true},
          "signed_at": {"type": "string", "format": "date-time"},
          "approval_digest": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
          "public_key_b64": {"type": "string", "minLength": 40},
          "signature_b64": {"type": "string", "minLength": 80}
        }
      }
    }
  }
}
