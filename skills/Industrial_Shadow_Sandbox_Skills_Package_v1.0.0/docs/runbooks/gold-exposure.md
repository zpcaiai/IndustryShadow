# Gold exposure

Revoke the identity and data key, stop evaluation workers, preserve access audit, and
identify every affected Gold version and derived output. Rotate the envelope key,
re-encrypt into a new immutable version, invalidate contaminated evaluations and
certificates, and test that normal diagnosis/tool/report paths cannot read Gold content.
Follow the applicable privacy notification process without copying Gold into tickets.
